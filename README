This little library contains python library to run various random walks.
It is under construction.
execute "bash compile_all.sh" to compile and run the test

The compile command file is there to ensure good functionning of whathever lsp is running.
