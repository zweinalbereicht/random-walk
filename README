This little library contains python library to run various random walks.
It is under construction.
execute "bash compile_all.sh" to compile.

The compile command file is there to ensure good functionning of whathever lsp is running since I'm using nvim's native lsp.
